[build]
  # We commit Flutter's web build output to git, so Netlify should just serve it.
  publish = "build/web"
  command = ""

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
force = true

